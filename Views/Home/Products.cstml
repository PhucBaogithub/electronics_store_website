                                    <div>
                                        <button class="btn btn-primary me-2" onclick="addToCart(${product.id})" 
                                                ${!product.isInStock ? 'disabled' : ''}>
                                            <i class="fas fa-cart-plus"></i> ${product.isInStock ? 'Add to Cart' : 'Out of Stock'}
                                        </button>
                                        <a href="/Home/ProductDetail/${product.id}" class="btn btn-outline-secondary">
                                            <i class="fas fa-eye"></i> View Details
                                        </a>
                                    </div> 